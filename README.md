# img2dots

Convert an image into Markdown-embedded LaTeX `\rule` dots — each pixel becomes a colored dot, rendered as a grid inside `$ … $`.

> Early scaffold. The conversion pipeline is under construction; see the roadmap for planned work.

## Usage (planned)

```
img2dots input.png -o output.md
```
